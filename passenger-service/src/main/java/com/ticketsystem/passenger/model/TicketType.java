package com.ticketsystem.passenger.model;

/**
 * Mirrors the ticket types accepted by printing-service's POST /tickets.
 */
public enum TicketType {
    ECONOMY,
    BUSINESS,
    FIRST_CLASS,
    VIP_PREMIUM
}
